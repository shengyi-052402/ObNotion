# Wiki Log

Append-only record of ingest, query, lint, and reorganization work.

## [2026-04-24] reorg | Initialize vault structure

Created `raw/`, `wiki/`, and `system/` scaffolding, added the schema in `AGENTS.md`, and initialized index/log/template files for future maintenance.
