# 日志

按时间追加记录摄入、查询沉淀、巡检和结构调整。

## [2026-04-24] reorg | Initialize vault structure

建立了 `raw/`、`wiki/`、`system/` 三层骨架，补充了 `AGENTS.md` 维护规则，并初始化了索引、日志和模板文件。

## [2026-04-24] reorg | 调整待处理资料入口

将待处理原始资料的入口从 `raw/inbox/` 调整为根目录下的 `Clippings/`，并同步更新相关说明文件。

## [2026-04-24] ingest | 批量处理现有剪藏资料

将 `Clippings/` 中现有的 Java、Spring、MySQL、Redis、操作系统、计算机网络等资料按主题整理为 `raw/sources/` 下的来源笔记，并在 `wiki/topics/` 下建立对应主题页，同时更新了总览与索引。
